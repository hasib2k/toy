Place your product review video here as `review.mp4`.

- Recommended formats: MP4 (H.264), WebM for fallback.
- File path used by the page: `assets/videos/review.mp4`.

If you prefer to use an external host (YouTube/Vimeo), replace the <video> element in `index.html` with an iframe embed or adjust `main.js` to open the external URL in the modal.